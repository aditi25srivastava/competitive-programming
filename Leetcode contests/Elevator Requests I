class Solution(object):
    def elevatorRequests(self, n, requests):
        """
        :type n: int
        :type requests: List[int]
        :rtype: int
        """
        curr = 0
        time = 0
        for floor in requests:
            time = time + abs(floor - curr)
            curr = floor
        return time    
        